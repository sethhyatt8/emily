# Copy to `.env` for local dev and for `npm run deploy` (embeds these at build time).
# For GitHub Actions Pages deploy, add the same keys as repository Secrets (same names).

VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key
